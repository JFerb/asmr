cmake_minimum_required(VERSION 3.16)
project(asmr_arm_description)

find_package(ament_cmake REQUIRED)

install(DIRECTORY
  urdf
  config
  worlds
  DESTINATION share/${PROJECT_NAME}
)

ament_package()
